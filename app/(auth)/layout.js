import React from 'react'

const AuthenticationLayout = ({children}) => {
  return (
    <div className='flex justify-center pt-40 bitems-center'>{children}</div>
  )
}

export default AuthenticationLayout